# TODO: Fix GitHub Action Cairo Dependency Error

- [ ] Update .github/workflows/ci.yml: Add a step to install 'libcairo2-dev' before installing Python dependencies.
- [ ] Update Dockerfile: Add 'libcairo2-dev' to the apt-get install in the builder stage.
